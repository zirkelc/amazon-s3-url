{
	"version": "0.2.0",
	"configurations": [
		{
			"type": "node-terminal",
			"name": "Debug (tsx)",
			"request": "launch",
			"command": "pnpm tsx ${relativeFile}",
			"cwd": "${workspaceFolder}"
		},
		{
			"type": "node-terminal",
			"name": "Vitest Test",
			"request": "launch",
			"command": "pnpm vitest ${relativeFile}",
			"cwd": "${workspaceRoot}"
		}
	]
}
